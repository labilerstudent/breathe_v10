void textParameter() {
  schriftart = loadFont("Keiner-Regular-48.vlw");
  // schriftart = createFont("SourceCodePro-Regular.ttf", 11);

  textFont(schriftart);
  textSize(20);
  textAlign(LEFT, CENTER);
}
